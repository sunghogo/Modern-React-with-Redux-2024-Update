/*
    - Typescript:
        - Helps you catch errors during development
        - Helps provide documenation for your components
        - Compiles to plain Javascript
        - Doesn't improve performance

    - Type annotations helps TS understand the data flowing through our app
    - Basic Types:
        - string, string[]
        - number, number[]
        - boolean, boolean[]
*/

const color: string = 'red';
const isRed: boolean = color === 'red';
const age: number = 25;

const colors: string[] = ['red', 'green', 'blue'];
const areColorsRed: boolean[] = [true, false, false];
const ages: number[] = [20, 30, 40];
